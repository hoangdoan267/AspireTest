import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  dotGroup: {
    flexDirection: 'row',
    marginRight: 18,
  },
  dot: {
    width: 8,
    height: 8,
    borderRadius: 8,
    marginRight: 6,
    backgroundColor: '#ffffff',
  },
});
